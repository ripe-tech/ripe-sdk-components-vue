{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@now/static-build",
            "config": {
                "distDir": "dist"
            }
        }
    ],
    "routes": [
        { "src": "/docs/(.*)", "dest": "/styleguide/$1" },
        { "src": "/(?!res)(.*)", "dest": "/storybook/index.html" }
    ]
}
