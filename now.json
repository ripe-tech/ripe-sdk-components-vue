{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@now/static-build",
            "config": {
                "distDir": "styleguide"
            }
        }
    ],
    "routes": [
        { "src": "/storybook", "dest": "/storybook/index.html" }
    ]
}
